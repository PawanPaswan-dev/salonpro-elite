# This folder holds static assets (favicon, logo, images) served at the site root.
# Currently the demo uses remote Unsplash images for galleries and hero sections,
# configured in next.config.js under images.remotePatterns.
#
# To use local assets, place files here (e.g. /public/logo.png) and reference
# them in code as "/logo.png".
